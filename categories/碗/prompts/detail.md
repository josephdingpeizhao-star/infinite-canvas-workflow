所有尺寸比例锁定必须写“口径约 {length_cm} 厘米、高度约 {height_cm} 厘米”。模块05是唯一尺寸标注图，只能标注用户确认的口径、高和已填写的宽，必须明确禁止容量、重量、材质等未确认参数，并且不得启用手持；其他模块不得启用尺寸标注信息。{optional_dimensions_detail}
{scene_safety_collective_rule}
场景规则句（如需复述必须原样）：{scene_rule}
详情页全批{handheld_count_text}启用手持，只能{handheld_phrase}，不得遮挡碗口、内壁、主要图案、圈足或组件关系。

【执行器内容合同】
1. 每套详情图的【输出画布比例】必须写“3:4”；【绑定角度槽位】必须写明唯一绑定源图及 A/B/C 槽位，不得使用 D 槽位。
2. 【标准模块归属】必须逐张写明模块01、模块02、模块03、模块04、模块05、模块06、模块07或模块08之一，并服从本批实际模块顺序。
3. 模块05必须是非手持图；【尺寸标注信息】与【尺寸标注图规则】都必须包含“高度约 {height_cm} 厘米”。必须明确禁止容量、宽度、直径、重量、材质等未经用户确认的信息；用户已确认的碗口径、高和可选宽仍按本批事实逐字保留，不得被该禁止句削弱。
4. 非模块05的【尺寸标注信息】与【尺寸标注图规则】都逐字写“非尺寸标注图”。
5. 每套都必须保留【手持交互声明】：禁用时逐字写“本张图不启用手持场景”；启用时必须包含“启用手持场景”，并按碗或组合单元的真实承托动作写明“静态握持”或“动态拿起”。
6. 每套都必须保留【动态手持样式参考图调用】：禁用手持时写“无”；静态握持时逐字写“无，仅动态拿起场景可调用”；动态拿起但未提供参考图时逐字写“未提供，不调用”。

【组合单元硬约束】
1. 原图中碗与配套盘，或多只碗成套同框时，整组视为同一个商品单元；身份档案必须记录组件构成、组件种类和件数。
2. 成品图必须锁死组件种类、数量和搭配关系；不得增减组件、拆分组合、替换配套件，也不得把任何商品组件当成道具。
3. 身份档案和后续 QC 必须逐项核对碗数和配套盘数；任一组件数量或搭配关系不一致即判定不合格并返修。

【HF-02 详情字段契约完整教学】

每一套【详情图单张变量配置】必须逐项输出下列 33 个字段。每个字段都要写入本张可执行取值，不得只复述字段名、写“按要求执行”或留空。

【标准模块归属】
写明本张对应淘宝天猫详情页的标准模块，必须使用模块01、模块02、模块03、模块04、模块05、模块06、模块07或模块08中的本批指定模块。
模块归属必须逐项服从【详情页模块覆盖计划】和本批配置编号顺序，不得跳号、重复冒充其他模块或自行改写模块编号。

【买家疑问】
写明本张要回答的一个具体买家问题，例如商品是否与主图一致、碗口和内壁是否清晰、圈足支撑是否真实、规格是否容易看懂或组合组件是否齐全。
疑问必须能够由当前角度、产品身份、已确认尺寸或可用资料回答，不得提出并回答没有证据的材质、功效、认证或售后问题。

【信息来源与可用证据】
写明本张文案和画面信息具体来自产品身份档案、绑定白底图、主图核心承诺、已确认尺寸、角度入库表和用户资料中的哪些证据。
组合单元必须把组件构成、组件种类、件数、叠放、承托和搭配关系列为证据边界。
如某类资料未提供，只写“未使用该类信息”，不得把“资料缺失”“未提供”或内部流程说明渲染到详情页画面；不得把风格参考图、辅助参考图或道具外观当成商品事实来源。

【平台硬约束检查】
固定包含：
商品名称、已确认尺寸、组件构成和卖点描述必须与主图、产品身份档案和用户确认事实一致；不得新增容量、其他尺寸、重量、具体材质、认证、品牌、型号或功效。
详情图输出画布比例固定为 3:4；图片交付建议宽度 1440px 时，对应单张交付图 1440x1920；单文件不超过 20MB，并避免二次压缩。
AI 场景化不得造成商品结构、材质、颜色、款式、大小、组件件数、搭配关系或效果失真，不得出现抠图贴图感、虚假标注或违反物理规律。

【绑定角度槽位】
写明本张变量配置调用的唯一合格 A/B/C 槽位、唯一绑定源图编号及角度入库表编号；字段正文必须原样包含“X 槽位”或“槽位 X”。
如果重复调用同一槽位，也要说明本张与同槽位其他配置的差异方向。
不得使用 D 槽位、缺失槽位、被拒绝源图或同时绑定多张白底图；组合单元必须沿用整组绑定图的组件构成和空间关系。

【角度适配原则】
用一句完整、可执行的话说明本张如何利用该角度完成详情页信息任务，并明确不得改变碗口可见程度、口沿透视、内壁与内底可见关系、碗身弧面、碗底或圈足可见关系。
若当前角度只能部分呈现某一结构，只能写“弱化展示”“辅助呈现”或“通过轮廓暗示”，不得强行补全不可见结构。
组合单元不得为适配角度而拆分、增减、替换组件或改变叠放、承托和搭配关系。

【产品角度依据】
固定写入：
本张产品角度以【绑定角度槽位】对应的白底碗图或组合单元整组白底图为唯一依据。商品在新画面中的朝向、俯仰关系、碗口与口沿可见程度、内壁与内底可见关系、碗身弧面透视、碗底或圈足可见关系，以及组合单元各组件的相对位置、叠放、承托和搭配关系，都必须参考该槽位对应的白底图。其他图片只用于确认产品身份和非颜色结构信息，不得改变本张图角度，不得倒置、翻转、拆分商品或补出绑定角度不可见的结构。

【产品颜色依据】
固定写入：
本张生成图片中的商品本体颜色，以【绑定角度槽位】对应的当次输入白底碗图或组合单元整组白底图作为唯一颜色参照。产品的基础色相、明暗深浅、饱和度倾向、图案颜色、材质本色和碗口、口沿、内壁、内底、碗身、碗底、圈足及各商品组件之间的颜色关系，必须参考该白底图。《产品身份档案》用于锁定产品结构、比例、材质、纹理、图案位置、组件关系、真实尺寸和身份描述；若身份档案中的颜色文字、风格母版、风格参考图、辅助参考图、场景光线、道具或背景色调与本张白底图的商品颜色观感不一致，以本张白底图为准。允许真实光照造成局部高光、阴影、反射和轻微明暗变化，但不得出现整体偏色、风格滤镜染色、冷暖色漂移、饱和度漂移或把商品或任一组件改成其他颜色。

【辅助参考图调用】
多角度单件合并参考图：有 / 无。
参考图名称或识别标记：如有则填写；无则写“无”。
对应产品：只原样填写本批 product_id；无则写“无”。
调用目的：仅用于增强碗口、口沿、内壁、内底、碗身弧面、碗底、圈足、图案延续、结构连接及组合组件关系的理解。
权限边界：本张产品角度仍以【绑定角度槽位】对应的白底图为唯一依据；辅助参考图不得改变本张商品朝向、俯仰、碗口可见程度、内壁透视、碗底或圈足可见关系、组合组件数量与搭配关系和真实尺寸。
颜色读取权限：禁止读取辅助参考图颜色；只读结构、轮廓、比例、图案位置和连接关系。
颜色冲突裁定：若辅助参考图与绑定白底图颜色不一致，以绑定白底图为准。

【页面任务】
说明本张详情图主要解释什么信息，必须是明确的详情页信息任务，不得写成主图点击任务或没有信息目标的纯氛围图。
页面任务必须适配本张标准模块和绑定角度，不得为完成任务而改变商品角度、结构、尺寸、图案或组合组件关系。

【展示重点】
说明本张要强化的碗口、口沿、内壁、内底、碗身弧面、图案、碗底、圈足、使用场景、比例关系或组合组件关系。
只写当前角度自然可见并有证据的信息；不可见或弱可见结构只能弱化展示、辅助呈现或通过轮廓暗示。
不得写需要改变商品朝向才能成立的细节，不得新增无证据卖点，也不得遗漏组合单元中的任何商品组件。

【镜头距离】
可以在远中景、中景、近景和局部特写之间选择，但必须服务当前角度自然可见的信息。
近景用于细节，中景用于整体，远中景用于氛围；局部特写只允许放大真实存在的结构和图案。
不得通过镜头距离、广角畸变或局部放大制造白底图中不存在的结构，也不得因特写裁掉组合单元中的商品组件。

【产品位置】
写明商品在画面中的位置以及为文字、标注和场景元素预留的空间。
详情图可以为信息区留白，但商品仍必须完整、清晰、可识别；组合单元必须整组保留，不得把任一组件裁出画面、虚化成背景或被文字、标注线、手部和道具遮挡。

【产品占比】
根据页面任务写明商品占画面的范围；细节页可以适当放大，场景页可以适当缩小，卖点说明页需要为文字预留空间。
不同详情图不得机械使用相同占比；即使是局部特写，也必须保留足够的商品和组合构成识别信息。
产品占比只代表构图，不得被解释为碗的真实口径、高度、组件件数或组件相对比例发生变化。

【尺寸比例锁定】
固定写入当批用户确认的“口径约 {length_cm} 厘米、高度约 {height_cm} 厘米”；用户另已确认宽时必须一并保留，不得改写口径和高度。
产品在画面中的占比只代表镜头距离和构图，不改变产品与手部、道具、桌面参照物及组合组件之间的现实尺寸关系。
不得根据白底图、手部、餐具、道具、风格参考图或辅助参考图反推新尺寸，也不得缩放某一组件去迁就标注或构图。

【输出画布比例】
固定写入“3:4”。
该值只规定最终详情图的宽高关系，不等同于【尺寸比例锁定】，不得用于改变商品真实口径、高度、碗身弧面、圈足比例、组件件数、组件相对比例或与手部、道具的现实大小关系。

【尺寸标注信息】
如果本张不是模块05，必须逐字写“非尺寸标注图”，不得启用尺寸标注信息。
如果本张是模块05，必须为非手持图，并填写尺寸来源、尺寸置信度、允许标注字段、禁止标注字段、允许出现的尺寸文字、禁止出现的尺寸文字和单位规则。
模块05必须包含“高度约 {height_cm} 厘米”，并逐字保留用户确认的口径、高和可选宽；同时必须明确写出禁止容量、宽度、直径、重量、材质等未经确认信息。如用户已填写宽度，“宽度”禁止项不得删除该已填写宽度，必须在同栏明确区分已填写宽度与“禁止另行编造宽度”。
所有数值必须来自用户确认事实，不得根据白底图、产品占比、手部或道具反推；组合组件没有独立确认尺寸时不得自行标注。

【尺寸标注图规则】
如果本张不是模块05，必须逐字写“非尺寸标注图”，并写明“不启用”。
如果本张是模块05，必须包含“高度约 {height_cm} 厘米”；尺寸标注线、箭头、虚线、括号线和参数框属于信息标注层，不属于道具，不计入道具数量。
标注线必须贴近商品的真实可见边界，不得遮挡碗口、口沿、内壁、内底、图案、圈足或组合组件关系；绑定角度无法自然展示的尺寸方向不得用箭头假装可见。
标注文字只能使用【尺寸标注信息】中列出的已确认文字，不得自行新增容量、其他尺寸、重量、材质、工艺、认证或功效，也不得为适配标注线拉伸、旋转、拆分或重绘商品。

【风格贴合锚点调用】
写明本张从《风格母版》【风格贴合锚点】中调用的锚点及其逐项落地方式；若某锚点与信息解释、商品识别、绑定角度、组合组件关系或真实感冲突，可不调用但必须写明原因。
每个锚点都必须同时写出锚点名称和本张的具体画面落点，例如环境物虚化层、布料或纸张前景、承托表面、少量低干扰点缀、主光方向或留白结构，不得只列锚点名称。
详情图必须结合本张信息任务调用锚点；结构、尺寸或局部特写任务可以减少无关场景元素，但不得删掉能证明风格一致性和组合完整性的主要锚点。
锚点只约束非产品视觉风格，不得改变产品身份、产品角度、尺寸比例、组合组件关系和商品本体颜色。
本字段所称风格参考图仅指用户提供或已有的原始风格参考图或成片；风格母版和场景光线不得让商品整体或某一组件偏冷、偏暖、偏灰、偏黄、偏绿或改变饱和度。

【道具密度等级】
从“极简 / 克制 / 常规 / 丰富生活场景 / 风格复现型”五档中写明本张密度等级，并参考《风格母版》的密度结论。
详情图可以按页面任务提高场景元素密度，但每个非产品元素都必须服务信息解释；不得堆叠无功能道具，也不得把“丰富生活场景”或“风格复现型”自动降为 1 至 3 个。
组合单元中的全部商品组件均不计入道具数量。

【背景层次配置】
当《风格母版》不是纯色或极简背景时，必须说明前景、中景、背景三层中至少两层的元素、空间位置和清晰或虚化程度；“风格复现型”必须说明三层。
不得无理由退化为纯白、纯灰或仅轻微渐变；结构说明、局部特写或尺寸标注任务需要简化背景时，必须写明简化理由。

【内容物状态】
依据当批高级选项和场景规则句，明确写明碗内为空置，或写明唯一获准出现的内容物及其静置状态；组合单元必须逐个说明各碗的状态，不得遗漏。
内容物属于使用场景元素，不属于商品身份，不得改变碗口、口沿、内壁、内底、碗身、碗底、圈足、图案、颜色或组合组件关系。
不得因碗的常见用途自行增加食物、清水、液体、倾倒、加热或其他未获准状态。

【构图方式】
写明本张采用的构图，例如左右信息分区、上商品下文字、商品居中留白、近景局部放大、生活场景纵深或俯视桌面构图。
构图可以调整商品位置、画面占比、前后景、文字区和道具关系，但不得改变商品的朝向、俯仰关系、碗口可见程度、透视关系或组合组件相对位置。
本批各详情图构图必须形成明确差异；尺寸标注图要区别于其他模块，但不得挤掉必要的信息或收尾任务。

【文字信息】
说明本张是否需要文字；需要时必须写明文字数量、位置、层级和不得遮挡的位置。
文字必须服务当前模块和当前角度能够证明的信息，不得宣称未确认的容量、其他尺寸、重量、具体材质、认证、品牌、功效、促销或组件数量承诺。
如不需要文字，必须写“本张图不设置文字信息”。

【中文营销文案】
如本张需要文字，逐条写出实际需要渲染的简体中文文案，并区分主标题、副标题和卖点短语。
文案只能使用商品名称、已确认尺寸、已确认组件构成、用户明确提供的信息和当前画面可见内容支持的表达；必须自然、简短，避免机器翻译腔、空泛夸张和无依据承诺。
如本张不需要文字，写“无”，不得把流程说明、资料缺失或禁止项当成画面文案。

【文字渲染要求】
说明文字位置、层级、字号相对关系、横排或竖排、字色与背景关系及不得遮挡的位置。
固定写入：最终出图只渲染【中文营销文案】和【尺寸标注信息】中列出的文字，不自行新增文字，也不得自行新增尺寸数字；文字必须清晰、无乱码、无错字、无变形、无漂浮，不得遮挡商品主体或组合组件关系。

【道具关系】
根据本张页面任务和【道具密度等级】，写出道具与非产品元素的具体安排；必须包含数量范围、允许类型、前景 / 中景 / 背景分布、与商品的距离、清晰或虚化程度及禁止遮挡位置。
道具必须服务信息解释、使用场景或空间层次，不得遮挡碗口、口沿、内壁、图案、圈足或组件关系，不得通过摆放引导商品改变角度。
配套盘、垫盘或同组碗被产品身份记录为商品组件时必须按主体处理，不得当成道具、删减、替换或另行计数；外部餐盘、托盘和其他场景物仍按道具处理。
非产品元素可保留自然褶皱、轻微纹理、微小摆放偏差和真实接触阴影，但不得把污渍、划痕、裂纹、破损、做旧、掉色或结构变形施加到商品本体。

【手持交互声明】
说明本张是否启用手持产品场景。
如果不启用，必须逐字写“本张图不启用手持场景”，本栏到此结束。
如果启用，必须包含“启用手持场景”，并完整写明“静态握持”或“动态拿起”、单手或双手、是否离桌及离桌幅度、真实尺寸摘要、接触和受力关系，以及为何不遮挡核心识别区、不改变绑定角度。
单碗只允许托住碗底或圈足、轻扶碗身外侧；组合单元必须整组托稳，不得只托其中一个组件、拆分组合、改变叠放或承托关系。
启用时还必须写明产品类型、手部入画方向、手指接触部位、产品真实口径、高度、可选宽和尺寸置信度；动态拿起只允许轻微离桌，并必须说明离桌幅度和桌面软阴影，静态握持则保持商品与承托面真实接触。
手部必须具备正常手指数、关节弯曲、皮肤纹理、受力逻辑和接触阴影；尺寸置信度低、组合承托不可靠或无法确认时不得启用手持，也不得用手部暗示具体大小。
手不得遮挡碗口、口沿、内壁、主要图案、圈足或组件关系；模块05默认不启用手持，手不计入【道具关系】数量。

【动态手持样式参考图调用】
如果本张不启用手持，写“无”。
如果本张启用静态握持，写“无，仅动态拿起场景可调用”。
如果本张启用动态拿起且未提供参考图，写“未提供，不调用”。
如果本张启用动态拿起且提供参考图，必须写明参考图名称、本张是否调用、调用目的、允许读取、禁止读取和冲突裁定。
只允许读取手部入画方向、手与碗或组合单元的相对位置、托举姿态、轻微离桌幅度、裁切方式和真实光影衔接；禁止读取产品身份、产品角度、产品颜色、产品尺寸、组件件数、品牌、文案、人物身份、皮肤特征、服饰、饰品或背景专属元素。
模块05本栏固定写“无”，不得调用动态手持样式参考图。

【背景与光线】
必须延续《风格母版》的整体规则，并根据【背景层次结构】写出前景、中景、背景各层的元素、明暗和虚化关系，不得只写“浅色背景、柔和光线”等空泛描述。
背景与光线必须服务当前模块，不得抢走商品主体注意力；只可影响非产品视觉风格和商品表面的真实局部高光、阴影与反射，不得给商品整体或某一组件套色调滤镜。
当风格母版不是纯色或极简背景时，不得把多层背景无故退化为纯白、纯灰或仅轻微渐变；结构、尺寸或局部特写任务需要简化背景时，必须说明保留了哪些锚点以及为何不会破坏整批视觉统一和组合完整性。
本字段所称风格参考图仅指用户提供或已有的原始风格参考图或成片。生活场景、使用场景、质感说明、比例说明和手持场景必须根据页面任务说明光源方向、接触阴影、前后景过渡及各组件的真实空间关系。
背景、桌面、布料、纸张、食物、道具、手部和环境光影可保留现实世界轻微不完美，但该规则不得作用于商品本体，不得改变结构、比例、颜色、纹理、图案和组件关系。
允许的轻微不完美包括自然褶皱、细微纹理、纸张轻微翘边、食物自然不规整、道具微小摆放偏差、手部自然皮肤纹理、真实接触阴影和轻微自然光影变化；不得因此形成脏乱、廉价杂乱、过度做旧或抢主体。

【真实感要求】
必须遵守《真实感约束》，画面应接近真实商业摄影，不得出现塑料感、过度磨皮、过亮反光、虚假景深、道具比例异常、手部畸形、文字漂浮、阴影不贴地或抠图贴图感。
局部特写只能放大真实存在的结构、图案和表面信息，不得创造原图不存在的污渍、划痕、裂纹、破损、做旧、掉色、图案磨损、口沿变形、圈足缺失或组件关系变化。
风格和光线只可改变非产品视觉氛围和真实局部明暗，不得改变商品本体颜色、结构、尺寸比例、组件件数和搭配关系。

【风格防退化检查】
对照《风格母版》自检本张是否保留应有的【道具密度等级】、非产品元素数量、前 / 中 / 后景层次和主要【风格贴合锚点】。
不得无故退化为纯白、纯灰、仅轻微渐变或单商品孤立棚拍；参考图无文字时，不得擅自添加与当前信息任务无关的大标题。
若因信息任务、商品识别、绑定角度、尺寸比例、组合组件关系或真实感需要偏离某一锚点，必须写明原因；不得借风格检查改变产品身份和角度。

【禁止事项】
固定写明：不得改变产品身份、绑定角度、结构、比例、颜色、纹理、图案、碗口、口沿、内壁、内底、碗身、碗底、圈足或组合组件关系。
不得增减组件、拆分组合、替换配套件、改变件数和搭配关系，也不得把任何商品组件当成道具。
不得强行补充当前角度不可见结构，不得让手部、文字、标注线或道具遮挡主体，不得生成拼图、宫格、多画面分区、复杂信息图或没有信息任务的纯氛围图。
不得新增容量、其他尺寸、重量、具体材质、认证、品牌、型号、功效、价格、促销或售后承诺。
不得把非产品元素的轻微真实瑕疵作用到商品本体；不得使用“高级感、精致感、氛围感”等无法执行的空泛描述，除非立即转化为具体画面规则。
