尺寸比例锁定必须写“口径约 {length_cm} 厘米、高约 {height_cm} 厘米”；不得补写容量、重量、具体材质、耐热、认证、品牌或型号。{optional_dimensions_main}
{scene_safety_collective_rule}
场景规则句（如需复述必须原样）：{scene_rule}
手持只能{handheld_phrase}，不能遮挡碗口、内壁、主要图案、圈足或组件关系，也不能改变绑定角度。

【执行器内容合同】
1. 每套主图的【输出画布比例】必须写“1:1”；【绑定角度槽位】必须写明唯一绑定源图及 A/B/C 槽位，不得使用 D 槽位。
2. 每套都必须保留【手持交互声明】：禁用时逐字写“本张图不启用手持场景”；启用时必须包含“启用手持场景”，并按碗或组合单元的真实承托动作写明“静态握持”或“动态拿起”。
3. 每套都必须保留【动态手持样式参考图调用】：禁用手持时写“无”；静态握持时逐字写“无，仅动态拿起场景可调用”；动态拿起但未提供参考图时逐字写“未提供，不调用”。

【组合单元硬约束】
1. 原图中碗与配套盘，或多只碗成套同框时，整组视为同一个商品单元；身份档案必须记录组件构成、组件种类和件数。
2. 成品图必须锁死组件种类、数量和搭配关系；不得增减组件、拆分组合、替换配套件，也不得把任何商品组件当成道具。
3. 身份档案和后续 QC 必须逐项核对碗数和配套盘数；任一组件数量或搭配关系不一致即判定不合格并返修。
