继续同一 final_prompts 套装主图批次任务。上一轮未通过编译期完整性校验；请完整重发同批全部配置的 JSON，不得引用、解释或局部修补上一轮正文，不得改变配置编号、顺序、套装角度与编排绑定、手持状态、页面任务或其他已确认事实。每一份 final_prompt 正文必须原样包含以下画布比例字面，禁止同义改写：
画布比例固定为 1:1
只返回原任务要求的完整 JSON 对象，不要 Markdown、代码围栏或额外说明。