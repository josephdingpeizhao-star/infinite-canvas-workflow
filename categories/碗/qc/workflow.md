碗品类沿用现有单品工作流、A/B/C/D 槽位、变量配置、最终提示词、只读 QC 和返修路由。主图与详情图数量读取本批确认事实，不使用固定张数。

形态 A 的碗盘组合单元仍作为一个商品走同一单品链路；不启用套装轨道，不读取分件白底输入，不生成套装身份档案或套装编排槽位。

品类差异只来自当前碗配方。任何配方文件缺失、无效或与批次 category 不一致，都必须在调用模型、创建图片作业或写正式产物前停止，不得回退其他品类规则。

QC 只判断现有图片与正式上游约束的一致性，不新增生成方向。实际配置编号、主图 1:1、详情图 3:4、产品身份、角度、尺寸、手持数量、场景边界、组件件数、搭配关系和平台规范均为硬约束。
