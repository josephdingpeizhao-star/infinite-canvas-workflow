盘子品类沿用单品 6 主图 + 8 详情图工作流、A/B/C/D 槽位、变量配置、最终提示词、只读 QC 和返修路由。

品类差异只来自当前盘子配方。任何配方文件缺失、无效或与批次 category 不一致，都必须在调用模型、创建图片作业或写正式产物前停止，不得回退杯类规则。

QC 只判断现有图片与正式上游约束的一致性，不新增生成方向。14 个配置编号、主图 1:1、详情图 3:4、产品身份、角度、尺寸、手持数量、场景边界和平台规范均为硬约束。
